load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "test",
    srcs = ["main.cpp"],
    deps = [
        "@folly//folly:folly",
    ],
)

test_suite(
    name = "tests",
    tests = [
        "@folly//folly/algorithm/test:binary_heap_test",
    ],
)
