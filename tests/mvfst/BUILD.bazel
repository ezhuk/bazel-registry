load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "test",
    srcs = ["main.cpp"],
    deps = ["@mvfst//quic:constants"],
)

test_suite(
    name = "tests",
    tests = [
        "@mvfst//quic/api/test:QuicSocketTest",
    ],
)
